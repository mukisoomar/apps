# Customer data
- has fields:
    name
    address
    phone
- Want to update and maintain customer data in a database
- Start of using a csv file withs synthetic data for the fields

# Synthetic data 
- Generate script for creating 100 records for synthetic data

# Application Requirements
- Create an application to display customer data and makes changes to it.
- Use API that will help replace csv file with database

# Programming Language
- Front end should be built using React JS
- Backend database is a csv  file that stores synthetic data
- Use node.js for creating the api code for managing data
- The Api should allow CRUD operations - create, Read, update, and Delete operations
- Front end should expose this functions to work with customer data. 

# UI Theme
- Use Dark theme

# Create a gitignore file to ignore on Github. Use industry standards